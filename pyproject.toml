[project]
name = "assignment1"
version = "0.1.0"
description = "Assignment 1 of MLOps - Section B"
authors = [
    { name = "m-ayan-asif", email = "m.ayanasif2003@gmail.com" }
]

requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
]

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
]
